replicaCount: 2
image:
  repository: ghcr.io/cyprus7/testtask/backend
  tag: "37e208e39a1c1b177b07928d73f3f7a3fd687be8"
imagePullSecrets:
  - name: regcred
service:
  type: ClusterIP
  port: 3000
config:
  nodeEnv: production
  redisHost: task-manager-redis-master
  redisPort: 6379
  port: 3000
  taskNotificationQueue: task-notifications
  dueSoonThresholdHours: 24
secret:
  create: false
  existingSecret: task-manager-secrets
  databaseUrlKey: DATABASE_URL
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
  hosts:
    - host: task20251104.test.chernov.us
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - task20251104.test.chernov.us
      secretName: task-manager-tls
