// Deprecated duplicate entrypoint. The real API entrypoint is `apps/api/src/index.ts`.
// Keep this file as a harmless module to avoid tooling that scans repo directories
// from attempting to compile or execute it in isolation.

export { };
