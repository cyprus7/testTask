image:
  repository: ghcr.io/cyprus7/testtask/tgbot
  tag: "b7f005c30c769c407b0b2f1c185023c820407e2b"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 8080
ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
  hosts:
    - host: task20251104.test.chernov.us
      paths:
        - path: /tg/webhook
          pathType: Prefix
  tls:
    - hosts: [task20251104.test.chernov.us]
      secretName: task-manager-tls
env:
  PUBLIC_URL: "https://task20251104.test.chernov.us"
  WEBHOOK_PATH: "/tg/webhook"
  # BOT_TOKEN will be sourced from a secret
