Thank you for installing the Task Manager chart! 🚀

Your release {{ .Release.Name }} is deploying the Bun/Elysia Task Manager API.

To verify the deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

To get the service details:
  kubectl get svc -n {{ .Release.Namespace }} {{ include "task-manager.fullname" . }}

If ingress is enabled, the API will be available at:
{{- range .Values.ingress.hosts }}
  - https://{{ .host }}
{{- end }}
